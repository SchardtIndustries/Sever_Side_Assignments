# Book Catalog Application (MongoDB)

## Requirements

- MongoDB Community Edition running locally (default: mongodb://localhost:27017)
- Python 3.10+ recommended

## Setup

```bash
cd Assignment_3
python3 -m venv .venv
source .venv/bin/activate
pip install -r requirements.txt
